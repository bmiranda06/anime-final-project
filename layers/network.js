(function () {
  window.SignalSelf = window.SignalSelf || {};
  window.SignalSelf.layers = window.SignalSelf.layers || {};

  window.SignalSelf.layers.network = {
    id: "network",
    label: "Network",
    onEnter() {
      // Placeholder hook for the network layer.
    },
  };
})();
